{
  "name": "appointments-lambda",
  "version": "1.0.0",
  "description": "AWS Lambda function for managing appointments",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "zip -r appointments-lambda.zip index.js package.json node_modules/"
  },
  "dependencies": {
    "mysql2": "^3.14.2"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
